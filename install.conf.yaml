- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.zprofile:
    ~/.zshrc:
    ~/.hyper.js:
      force:  true

- brewfile:
    - Brewfile

- shell:
  - [git submodule update --init --recursive, Installing submodules]
