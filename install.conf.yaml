- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.zprofile:
    ~/.zshrc:
    ~/.hyper.js:
      force:  true

- shell:
  - [git submodule update --init --recursive, Installing submodules]
